<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="OnlineDanceStore.View.Categories.DancingShoes"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
     xmlns:models="clr-namespace:OnlineDanceStore.Models"
    xmlns:viewmodels="clr-namespace:OnlineDanceStore.ViewModels"
    x:DataType="viewmodels:CategoriesViewModel"
    Title="DancingShoes">

    <!--<VerticalStackLayout>
        <Label
            HorizontalOptions="Center"
            Text="What Are You Looking For"
            VerticalOptions="Center" />
        <CollectionView EmptyView="No shoes found" ItemsSource="{Binding Items}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Item">
                    <StackLayout>
                        <Label Text="{Binding Description}" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>
    </VerticalStackLayout>-->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        
        <Border BackgroundColor="Black" WidthRequest="3" Padding="5" >
            <Grid>
                <Label   Grid.Row="0" HorizontalOptions="Center"
 Text="What Are You Looking For"
 VerticalOptions="Center" />

        
        <CollectionView EmptyView="No shoes found" ItemsSource="{Binding Items}">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" Span="2" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Item">
                   
                    <StackLayout>
                                <Label Text="{Binding Name}"  Grid.Row="1" Grid.Column="0" />
                        <Label Text="{Binding SizeItem.Size}" />
                        <Label Text="{Binding ColorItem.Id}" />
                        <Label Text="{Binding Price}" />
                        <Label Text="{Binding Description}" />
                                <Button x:DataType="viewmodels:ShoppingCartViewModel"
                                    Text="Add Item" Command="{Binding AddToCartCommand}" />
                            </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        </Grid>
        </Border>
    </Grid>

</ContentPage>