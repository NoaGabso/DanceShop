<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="OnlineDanceStore.View.Categories.DancingShoes"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:models="clr-namespace:OnlineDanceStore.Models"
    xmlns:viewmodels="clr-namespace:OnlineDanceStore.ViewModels"
    Title="DancingShoes"
    x:DataType="viewmodels:CategoriesViewModel">

    <!--<VerticalStackLayout>
        <Label
            HorizontalOptions="Center"
            Text="What Are You Looking For"
            VerticalOptions="Center" />
        <CollectionView EmptyView="No shoes found" ItemsSource="{Binding Items}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Item">
                    <StackLayout>
                        <Label Text="{Binding Description}" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>
    </VerticalStackLayout>-->


    <Grid Margin="20" HorizontalOptions="CenterAndExpand">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <Label
            Grid.Row="0"
            HorizontalOptions="Center"
            Text="What Are You Looking For"
            VerticalOptions="Center" />
        <Border
            Grid.Row="1"
            Padding="5"
            BackgroundColor="AliceBlue"
            HeightRequest="200"
            WidthRequest="300">
            <Grid>


                <CollectionView ItemsSource="{Binding Items}">
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical" />
                    </CollectionView.ItemsLayout>
                    <CollectionView.EmptyView>
                        <StackLayout>
                            <Label Text="No Shoes Found" />
                        </StackLayout>
                    </CollectionView.EmptyView>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:Item">

                            <StackLayout HeightRequest="200">
                                <Label
                                    Grid.Row="1"
                                    Grid.Column="0"
                                    Text="{Binding Name}" />
                                <Label Text="{Binding SizeItem.Size}" />
                                <Label Text="{Binding ColorItem.Id}" />
                                <Label Text="{Binding Price}" />
                                <Label Text="{Binding Description}" />
                                <Button
                                    x:DataType="viewmodels:ShoppingCartViewModel"
                                    Command="{Binding AddToCartCommand}"
                                    Text="Add Item" />
                            </StackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Grid>
        </Border>
    </Grid>

</ContentPage>